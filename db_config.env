# Database Configuration for Tarneeb Games Sync
# Uses DATABASE_USERNAME and DATABASE_PASSWORD environment variables

DB_HOST="localhost"
DB_PORT="5432"
DB_NAME="amirhessam_main"
TABLE_NAME="tarneeb.games"

# Optional: Set to 1 to enable verbose logging
VERBOSE=0

# Optional: Set to 1 to enable dry run (test mode)
DRY_RUN=0
